import {StyleSheet} from 'react-native';

const body = StyleSheet.create({
  backgroundColor: 'A8AEC1',
});

const dropdown = StyleSheet.create({
  margin: 10,
  width: '60%',
});

const dropdownYear = StyleSheet.create({
  width: '30%',
  margin: 10,
});

export {body, dropdown, dropdownYear};
