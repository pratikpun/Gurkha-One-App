import {StyleSheet} from 'react-native';

const background = StyleSheet.create({
  backgroundColor: '#FFFFFF',
  height: '100%',
  //width: '100%',
});

const details = StyleSheet.create({
  margin: 30,
  fontSize: 20,
  borderBottomColor: 'black',
  borderBottomWidth: 1,
  color: 'black',
});

const text = StyleSheet.create({
  marginLeft: 30,
  marginTop: 10,
  fontWeight: 'bold',
  fontSize: 18,
  color: 'black',
});

const login = StyleSheet.create({
  marginTop: 20,
  color: 'black',
});

const border = StyleSheet.create({
  margin: 30,
  fontSize: 20,
  borderBottomColor: 'black',
  borderBottomWidth: 1,
  color: 'black',
});

export {background, details, text, login, border};
